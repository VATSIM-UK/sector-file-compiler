namespace Compiler.Output
{
    /*
     * Subsections of the main output sections. They do not have a header
     * and function as a logical divide of data.
     */
    public enum Subsections
    {
        // Global
        DEFAULT,

        // SCT

        // ESE
        ESE_AIRSPACE_SECTORLINE,
        ESE_AIRSPACE_SECTOR,
        ESE_AIRSPACE_COORDINATION
    }
}
